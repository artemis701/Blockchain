// import { createSelector, createStructuredSelector } from "reselect";


//Store Selectors
export const walletState = (state) => state.wallet.walletState;
export const refreshState = (state) => state.wallet.refreshState;
export const lotteryState = (state) => state.wallet.lotteryState;